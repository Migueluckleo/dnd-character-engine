# Copy this file to .env for local/backend runtime only.
# Never commit .env with real credentials.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/dnd_engine?schema=public"
AUTH_SECRET="replace-with-a-long-random-secret"
PORT=3000
NODE_ENV=development
NODE_VERSION=20
ALLOWED_ORIGINS="http://localhost:5500,http://127.0.0.1:5500,https://www.migueleo.com"
